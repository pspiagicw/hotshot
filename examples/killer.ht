($ intfreq 
   (@ 0 
      (| 
        (cat /proc/cpuinfo)
        (grep "Mhz")
        (awk "{ s += 4 END { print substr(s/8000,1,3)} } "))))


(if (= (arg 1) "--integer")
  (? "$(intfreq)Ghz" intfreq)
  (do 
    (if (< intfreq 1)
    )
)

