(fn add (x y)
   (+ x y))

(add 2 1)
(add 1 2)


(fn varadd (x)
   (+ x))

(fn print (x) 
   (? x)) ;; Returns () ;; 

(bool (print 2)) ;; Should evaluate false ;;

(map 
  (# (x) (? x))
  [2 1 2 3]
  )

(! addF (x y)
   (+ (x y) y)) ;; First class functions! ;; 

(addF (# (x) (+ x 2)))

(! (x) (? x)) ;; Lambda functions ;;
