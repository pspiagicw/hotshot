(fn fizzbuzz (n)
  (cond ((and (= (mod n 3) 0) (= (mod n 5) 0)) "FizzBuzz")
        ((= (mod n 3) 0) "Fizz")
        ((= (mod n 5) 0) "Buzz")
        (t (write-to-string n))))

(dotimes (i 100)
  (format t "~a~%" (fizz-buzz (1+ i))))
